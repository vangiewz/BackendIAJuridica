[pytest]
markers =
    ia_local: prueba opcional contra Ollama local; activar con RUN_IA_LOCAL=1
    integracion: toca Postgres de verdad (necesita DATABASE_URL). La suite offline es
        `pytest -m "not integracion"` y no debe depender de la red.
